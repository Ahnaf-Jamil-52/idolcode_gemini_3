# Coach Engine Dependencies
# Core (no ML - minimal dependencies)
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Optional: Local sentiment model (uncomment if using)
# transformers>=4.30.0
# torch>=2.0.0

# Optional: LLM API calls (uncomment if using)
# httpx>=0.24.0
# openai>=1.0.0
google-generativeai>=0.3.0  # For Gemini AI integration

# For web integration (if building API)
# fastapi>=0.100.0
# uvicorn>=0.22.0
# pydantic>=2.0.0
# websockets>=11.0.0

# For MongoDB storage (optional)
# pymongo>=4.0.0
# motor>=3.0.0  # async MongoDB

